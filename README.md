# Hibernate lazy fetch problem

##Prerequisites:
Docker needs to be installed

##How to
1. Clone this repository
1. run `docker-compose up` from the root directory 
2. run `gradlew bootRun`
3. Watch the console output from hibernate